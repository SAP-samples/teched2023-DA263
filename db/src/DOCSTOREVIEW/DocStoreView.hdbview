view DOCSTOREVIEW as  
(SELECT 
       PID,
       AVG(RATING)     as AVGRATING,
       COUNT(RATING)   as COUNTRATING,
       MIN(RATING)     as MINRATING,
       MAX(RATING)     as MAXRATING
FROM 
       DOCSTOREVIEW_BASE 
GROUP BY 
       PID )
; 