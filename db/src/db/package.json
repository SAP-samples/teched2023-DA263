{
  "name": "deploy",
  "dependencies": {
    "@sap/hdi-deploy": "^4.*"
  },
  "scripts": {
    "start": "node node_modules/@sap/hdi-deploy/deploy.js --parameter com.sap.hana.di.table/try_fast_migration=true"
  }
}
